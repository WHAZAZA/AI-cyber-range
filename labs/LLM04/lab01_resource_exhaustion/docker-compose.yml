services:
  app:
    build:
      context: ../../..
      dockerfile: dockerfiles/LLM04_Model_Denial_of_Service/easy/Dockerfile
    ports:
      - "${HOST_PORT:-8004}:8000"
    environment:
      - LAB_ID=LLM04_lab01_resource_exhaustion
      - DIFFICULTY=${DIFFICULTY:-easy}
      - FLAG_SHA256=${FLAG_SHA256}
      - FLAG_PLAIN=${FLAG_PLAIN}
