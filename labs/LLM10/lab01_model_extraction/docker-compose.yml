services:
  app:
    build:
      context: ../../..
      dockerfile: dockerfiles/LLM10_Model_Theft/easy/Dockerfile
    ports:
      - "${HOST_PORT:-8010}:8000"
    environment:
      - LAB_ID=LLM10_lab01_model_extraction
      - DIFFICULTY=${DIFFICULTY:-easy}
      - FLAG_SHA256=${FLAG_SHA256}
      - FLAG_PLAIN=${FLAG_PLAIN}
